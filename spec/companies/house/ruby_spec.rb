require 'spec_helper'

describe Companies::House::Ruby do
  it 'has a version number' do
    expect(Companies::House::Ruby::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
